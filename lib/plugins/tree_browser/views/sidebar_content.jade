#inter-tree-browser

script
  var Contents = new kendo.data.HierarchicalDataSource({
    type: "odata",
    transport: {
      read: {
        url: inter.host.master+'/contents'
      }
    },
    schema: {
        model: {
            hasChildren: "Contents",
            id: "CategoryID",
            children: Products
        }
    }
  });

  $("#treeview").kendoTreeView({
    dataSource: Categories,
    dataTextField: ["Contents"]
  });