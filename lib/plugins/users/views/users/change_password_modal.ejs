iokit.modal( <%- JSON.stringify( partial( partialPath, {user: user, currentUser: currentUser} ) ) %>, {
              title: '<%= t('user.change_password') %>',
              height: 300,
              completed: function( modal ){
                modal.find('input[type=password]:first').focus();
              }
});